<h1><%=@cat.name%></h1>
<%=button_to "back to all", cats_path, method: 'get'%>
<!--insert kitty pic here-->
<%= image_tag "kitten_test.jpg", height: '150', width: '150'%>

<br><br>
<h2 style="margin: 0; display: inline;">Owner:</h2>
<!--link to owner-->
<h3 style="margin: 0; display: inline;" align="right"><%=@cat.owner.name%></h3>
<br>
<h2 style="margin: 0; display: inline;">Breed:</h2>
<h3 style="margin: 0; display: inline;" align="right"><%=@cat.breed%></h3>
<br>
<h2 style="margin: 0; display: inline;">Price:</h2>
<h3 style="margin: 0; display: inline;" align="right">$<%=@cat.price%></h3>
<br>
<h2 style="margin: 0; display: inline;">Neighborhood:</h2>
<h3 style="margin: 0; display: inline;" align="right"><%=@cat.neighborhood%></h3>
<br>
 <h2 style="margin: 0; display: inline;">Rating:</h2>
  <% if @cat.overall_rating > 0 %>
    <p style="margin: 0; display: inline;" align="right"><%=("⭐️" * @cat.overall_rating.to_i)+("☆" * (5 - @cat.overall_rating.to_i))%>
    <%="#{@cat.overall_rating} / 5"%></p>
  <% else %>
    None
  <% end %>
  <br>
<p>Tags: 
  <%if @cat.tags.length > 0%>
    <%@cat.tags.each do |tag|%>
    #<%=link_to tag.name, "/cats/?q=#{tag.name}"%> 
    <%end%>
  <%else%>
      None
  <%end%>
<%= button_to "Rent This Cat", new_reservation_path(@cat), method: :get %>
</p>
<%if current_user && current_user.id == @cat.owner_id.to_i%>
  <%= button_to 'Edit ' + @cat.name, edit_cat_path(@cat), method: :get%>
  <%= button_to 'Retire ' + @cat.name, cat_path(@cat), method: 'delete'%>
<%end%>
<h2>Reviews of <%= @cat.name %></h2>
<% if @reviews %>
<%@reviews.each do |review|%>
     <%= link_to review.reservation.renter.name, review.reservation.renter.name %>
    <p><%=("⭐️" * review.rating)+("☆" * (5 - review.rating))%><%= review.rating.to_f %>/5</p>
    <p>"<%= review.review %>"</p>
  <%end%>
<% else %>
  None
<% end %>
